import React from "react";
import PropTypes from "prop-types";

const ProfileTop = ({ profile }) => {
  return <div>Place Holder for main profile (Avatar to be added)</div>;
};

ProfileTop.propTypes = {};

export default ProfileTop;
